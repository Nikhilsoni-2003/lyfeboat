const PROTOCOL = "https";
const HOST = "localhost:8001";
const SUFFIX = "api";

// const PROTOCOL = "https";
// const HOST = "uat.lyfeboat.in";
// const SUFFIX = "api";

export const API_BASE_URL = `${PROTOCOL}://${HOST}/${SUFFIX}`;

export const SCHEMA_API_BASE_URL = `${PROTOCOL}://${HOST}/${SUFFIX}`;
